---
layout: layouts/base.njk
title: Movies
description: Movie reviews on Whyyoushould. Streaming and theatrical recommendations from critics who respect your time.
---

<div class="collection-header">
  <h1>Movies</h1>
  <p>Definitive film recommendations</p>
</div>

<section class="collection-grid">
  {% for review in reviews %}
  {% if review.type === "movie" %}
  <a href="{{ ('/reviews/' ~ review.slug ~ '/') | url }}" class="collection-card">
    {% if review.poster_path %}
    <div class="collection-card-poster">
      <img src="{{ review.poster_path }}" alt="{{ review.title }}" />
      <div class="collection-card-verdict verdict-{{ review.verdict | verdictClass }}">
        <span class="verdict-text">{{ review.verdict }}</span>
      </div>
    </div>
    {% else %}
    <div class="collection-card-poster-placeholder">??</div>
    {% endif %}
    <div class="collection-card-info">
      <h2>{{ review.title }}</h2>
      <p class="collection-card-creator">{{ review.creator }}</p>
      <div class="collection-card-meta">
        <span class="collection-card-rating">{{ review.rating }}/10</span>
        <span class="collection-card-year">{{ review.releaseYear }}</span>
      </div>
      <p class="collection-card-excerpt">{{ review.why }}</p>
    </div>
  </a>
  {% endif %}
  {% endfor %}
</section>
